import 'package:flutter/material.dart';

class clase extends StatefulWidget {
  @override
  claseApp createState() => claseApp();
}

class claseApp extends State<clase> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Template'),
      ),
    );
  }
}
